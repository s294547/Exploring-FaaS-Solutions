apiVersion: v1
kind: ServiceAccount
metadata:
  name: grafana-operator
  labels: {{ include "grafana-operator.app.labels" . | nindent 4 }}
    app.kubernetes.io/component: operator
    name: grafana-operator
    user-action-pod: "true"
automountServiceAccountToken: true